version: 0.1

phases:
  pre_build:
    commands:
      - npm install
  build:
    commands:
      - npm run webpack

artifacts:
  files:
    - index.html
    - favicon.ico
    - Benjamin_Geyer_Resume.pdf
    - styles/main.css
    - images/*
    - dist/*